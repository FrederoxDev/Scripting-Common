{
  "name": "scripting-common",
  "version": "1.0.8",
  "description": "Generic helper code for the MCBE Scripting-API's",
  "author": "FrederoxDev",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.1.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "release": "npm version patch && git push && tsc && npm publish",
    "build": "tsc"
  },
  "types": "dist/Index.d.ts",
  "main": "dist/Index.js",
  "dependencies": {
    "@minecraft/server": "^1.11.0"
  }
}
